{
  "type": "object",
  "title": "PartnerBillDocx",
  "description": "Résultats de la conversion d'une facture à destination d'un partenaire depuis JSON vers Word (docx).",
  "properties": {
    "bill": {
      "type": "string"
    }
  },
  "required": [
    "bill"
  ]
}