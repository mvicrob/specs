{
  "title": "CustomProfile",
  "type": "object",
  "description": "Prêt ou plan progressif variant par paliers.",
  "properties": {
    "type": {
      "type": "string",
      "default": "CUSTOM",
      "description": "Type de profil de remboursement.",
      "enum": [
        "CUSTOM"
      ]
    },
    "steps": {
      "type": "array",
      "items": {
        "$ref": "./Step.v1.json"
      }
    }
  },
  "required": [
    "type"
  ]
}